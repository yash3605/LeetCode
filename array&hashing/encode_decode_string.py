class Solution:

    def encode(self, strs: list[str]) -> str:
        return ""

    def decode(self, s: str) -> list[str]:
        return []

solution = Solution()
input = ["we", "say", ":", "yes"]
encode = solution.encode(input)
